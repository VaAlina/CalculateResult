CalculateResult 1.5 Запрашивает уравнение и выводит второе число и 2 преобразованных в строки массива на экран.
===================

package toString;

import java.util.Scanner;

public class CompareAllNumInArray {
public static void main(String[] args) {
System.out.println("Введите пример.");
Scanner scanner = new Scanner(System.in);
        String a = scanner.nextLine();
        
        String[] parts = a.split("\\D");
        String[] operators = a.split("\\d+");
        System.out.println(parts.length);//Печать количества символов.
        System.out.println(java.util.Arrays.toString(parts));
        System.out.println(java.util.Arrays.toString(operators));
        
    }
      
}

CalculateResult 1.4 Запрашивает уравнение и выводит второе число и 2 преобразованных в строки массива на экран.
===================
8+9
9
[8, 9]
[, +]
package toString;

import java.util.Scanner;

public class CompareAllNumInArray {
	public static void main(String[] args) {
		System.out.println("Введите пример.");
		Scanner scanner = new Scanner(System.in);
        String a = scanner.nextLine();
        
        String[] parts = a.split("\\D");
        String[] operators = a.split("\\d+");
        System.out.println(a.parts[1]);//Тут ошибка, но программа работает.
        System.out.println(java.util.Arrays.toString(parts));
        System.out.println(java.util.Arrays.toString(operators));
        
    }
      
}


CalculateResult 1.3 Запрашивает уравнение и выводит 2 преобразованных в строки массива на экран.
===================
package toString;

import java.util.Scanner;

public class CompareAllNumInArray {
	public static void main(String[] args) {
		System.out.println("Введите пример.");
		Scanner scanner = new Scanner(System.in);
        String a = scanner.nextLine();
        
        String[] parts = a.split("\\D");
        String[] operators = a.split("\\d+");
        System.out.println(java.util.Arrays.toString(parts));
        System.out.println(java.util.Arrays.toString(operators));

    }
      
}


CalculateResult 1.2 Запрашивает уравнение и выводит нужную мне часть на экран.
===================
package toString;

import java.util.Scanner;

public class CompareAllNumInArray {
	public static void main(String[] args) {
		System.out.println("Введите пример.");
		Scanner scanner = new Scanner(System.in);
        String a = scanner.nextLine();
        String[] parts = a.split("\\+");
        String part1 = parts[0];
        String part2 = parts[1];
        System.out.println(part1);

    }
      
}


CalculateResult 1.1 //Не печатает результат.
===================
package toString;

import java.util.*;

public class CalculateResult {
	int x, y;
	String op;


	public static void main(String[] args){
		int x, y, r;
		String op;
		Scanner scanner = new Scanner(System.in);
		
	    x = scanner.nextInt();
		op = scanner.next();
		y = scanner.nextInt();
		
		switch(op){
		case "+": r = x + y;
		case "-": r = x - y;
		case "*": r = x * y;
		case "/": r = x / y;
		System.out.println(r);
		}
		//System.out.println(r);
	}
}



CalculateResult 1.0 // Считывает введённые символы.
===============

import java.util.*;

public class CalculateResult {
	int x, y;
	String op;


	public static void main(String[] args){
		int x, y;
		String op;
		Scanner scanner = new Scanner(System.in);
		
	  x = scanner.nextInt();
		op = scanner.next();
		y = scanner.nextInt();
		
	}
}

